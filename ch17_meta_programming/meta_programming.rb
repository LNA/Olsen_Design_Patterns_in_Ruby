# Allows us to take advantage of Ruby's dynamic behaviors by
# modifying the following at runtime:
# => An existing class,
# => The behavior of an object independently of its class, and
# => Evaluate strings as Ruby code